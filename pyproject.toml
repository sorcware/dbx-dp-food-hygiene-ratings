[project]
name = "dbx-dp-food-hygiene-ratings"
version = "1.0.0"
description = "Basic ingestion for Food hygiene ratings in the UK using custom pyspark data sources and declarative pipelines."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "databricks-connect>=18.1.2",
]
